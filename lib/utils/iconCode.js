const logSymbols = require('log-symbols');

const iconInfo = logSymbols.info
const iconError = logSymbols.error
const iconSuccess = logSymbols.success
const iconWarning = logSymbols.warning
module.exports = {
  iconInfo,
  iconError,
  iconSuccess,
  iconWarning

}
